4. Program to add two numbers, memory to store information must be acquired using
DYNAMIC MEMORY ALLOCATION techniques .
Program 4
#include <stdio.h>
#include <stdlib.h>
int main()
{
int*a,*b,*sum;
a=(int*)malloc(sizeof(int));
b=(int*)malloc(sizeof(int));
sum=(int*)malloc(sizeof(int));
printf("Enter the values\n");
scanf("%d %d",a,b);
*sum = *a + *b;printf("sum is %d",*sum);
return 0;
}
